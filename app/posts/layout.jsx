//import { Counter } from './Counter.jsx'

export default function PostsLayout ({ children }) {
    return (
        <div>
            <small> Home - Posts</small>
            {children}
        </div>
    )
}